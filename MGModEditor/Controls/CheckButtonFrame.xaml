<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:MGEditor.Controls"
    >

    <Style TargetType="{x:Type controls:CheckButtonFrame}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:CheckButtonFrame}">
                    <Grid>
                        <Border
                            Padding="5"
                            Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1,1,1,1"
                            CornerRadius="5,5,5,5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition MinWidth="200" Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <ui:Button
                                    Content="{TemplateBinding ButtonContent}"
                                    Command="{Binding ToggleCheckCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:CheckButtonFrame}}}"
                                    Grid.Column="0"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Center"
                                    VerticalContentAlignment="Center"
                                    MinHeight="36"
                                    Margin="20,0,0,0">
                                    <ui:Button.ContentTemplate>
                                        <DataTemplate>
                                            <TextBlock
                                                Text="{Binding}"
                                                TextWrapping="Wrap"
                                                TextTrimming="CharacterEllipsis"
                                                TextAlignment="Center" />
                                        </DataTemplate>
                                    </ui:Button.ContentTemplate>
                                </ui:Button>
                                <ui:ToggleSwitch 
                                    Grid.Column="1"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Margin="0,0,20,0"
                                    IsChecked="{Binding IsChecked, RelativeSource={RelativeSource AncestorType={x:Type controls:CheckButtonFrame}}}"
                                    OffContent="{DynamicResource Common.No}" OnContent="{DynamicResource Common.Yes}"/>
                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>
